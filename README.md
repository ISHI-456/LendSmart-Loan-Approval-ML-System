Built an end-to-end supervised machine learning pipeline for predicting loan approval. Performed Exploratory Data Analysis (EDA), feature engineering, and implemented multiple classification models including Logistic Regression, Decision Tree, Random Forest, and XGBoost. Evaluated models using Accuracy, Precision, Recall, and F1-score, and selected XGBoost as the final model for prediction due to its superior performance.
